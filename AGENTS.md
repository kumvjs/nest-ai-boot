<!-- kuvibe:managed:start template=agents-router revision=1 -->
# Agent Router

Before non-trivial work:

1. Read `kuVibe.md` and `.agents/kuvibe.yaml`; stop version writes on an unsupported schema/release.
2. Read `.agents/project.md` and `.agents/context/stack.md`.
3. Select the relevant workflow under `.agents/workflows/`.
4. For active scoped work, read the matching directory under `.agents/notes/active/` and keep its artifacts current.
5. Preserve project-owned documentation and existing user changes. Use Git history as recovery; never regenerate project knowledge during refresh/migration.

Current active work: `.agents/notes/active/vben-api-compatibility/`.
<!-- kuvibe:managed:end -->
